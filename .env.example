# Azure MCP Server Docker Compose Environment Variables
# Copy this file to .env and fill in your values
#
# Required: Azure authentication credentials
# See: https://learn.microsoft.com/dotnet/api/azure.identity.environmentcredential

# Azure Tenant ID (required)
AZURE_TENANT_ID=your-tenant-id

# Azure Client ID / Application ID (required)
AZURE_CLIENT_ID=your-client-id

# Azure Client Secret (required for service principal auth)
AZURE_CLIENT_SECRET=your-client-secret

# Optional: Default Azure Subscription ID
AZURE_SUBSCRIPTION_ID=

# Optional: MCP Server configuration
# Port to expose the MCP server (default: 8080)
MCP_PORT=8080

# Server mode: single, namespace (default), all, or consolidated
MCP_MODE=namespace

# Set to any value to enable read-only mode (e.g., MCP_READ_ONLY=true)
# MCP_READ_ONLY=

# ASP.NET Core environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

# Optional: Storage-only service port (when using storage-only profile)
MCP_STORAGE_PORT=8081
